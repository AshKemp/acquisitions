# Shared environment template
# For Docker workflows, use .env.development and .env.production.

PORT=3000
NODE_ENV=development
LOG_LEVEL=info
JWT_SECRET=replace_with_jwt_secret
ARCJET_KEY=replace_with_arcjet_key
DATABASE_URL=postgresql://<username>:<password>@<project>.neon.tech/<database>?sslmode=require
